package arr

import (
	"fmt"
	"testing"
)

func TestRemoveElement(t *testing.T) {
	/*l := removeElement([]int{1}, 1)
	println(l)*/
	nums := []int{9, 2, 1, 3}
	for i := range nums {
		fmt.Println(nums[i])
	}
}
